A simple bootstrap Clojurescript project. Include a REPL, a NREPL for Vim to connect to it (basically a REPL with a socket) with cidr/nrepl and cidr/piggiepack.

To run dev: `clojure -M:dev`
To compile for prod: `clojure -M:prod`
